SUPERSEDED 2026-08-29, DO NOT MERGE THIS BRANCH.

This branch's Y1 page reads/writes its own y1_money_map_state table via a
y1_upsert RPC. Boubacar corrected that same-day design: every 12-week cycle
lives in ONE permanent shared table forever, scoped by board_id, never a
new table per year.

The corrected page is on branch fix/money-map-y1-unify-2026-08-29, which
repoints this exact file's Supabase calls to the shared y0_upsert RPC /
y0_money_map_state table (board_id stays 'y1'). That branch is built off
this one's tip (18c8477) so its diff is a clean superset -- merge THAT
branch instead of this one, or merge this one first and then immediately
follow with fix/money-map-y1-unify-2026-08-29's one-file diff.

See agentsHQ memory/reference_year_one_money_map.md for the full design.
